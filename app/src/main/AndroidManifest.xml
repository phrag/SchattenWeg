<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Fail-closed hardening. MapLibre's library manifest requests INTERNET,
         ACCESS_WIFI_STATE and both location permissions; the manifest merger
         would hand them to us silently. We use none of them, so strip them:
         with no INTERNET an accidental socket throws instead of leaking, and
         the app asks for no dangerous permission at all.

         ACCESS_NETWORK_STATE is deliberately kept — MapLibre's
         ConnectivityReceiver calls ConnectivityManager and would hit a
         SecurityException without it. It is a normal (non-dangerous)
         permission and moves no data off the device.

         If the map's location puck is ever enabled, re-add the location
         permissions here as a conscious decision, and use the platform
         LocationManager (GNSS) — never the Fused Location Provider. -->
    <uses-permission
        android:name="android.permission.INTERNET"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_WIFI_STATE"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"
        tools:node="remove" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.Schattenweg">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
