# Rust
target/
Cargo.lock.orig

# Everything under data/ is generated by scripts/build_map_assets.sh and is
# reproducible from Geofabrik: the raw extract, the filtered snapshot, the
# Planetiler jar and its sources, and the rendered tiles. None of it belongs
# in git -- several files are tens to hundreds of MB.
data/
*.pmtiles
*.mbtiles
*.part

# Android / Gradle
.gradle/
build/
local.properties
captures/
app/src/main/jniLibs/
app/src/main/java/uniffi/
app/src/main/assets/map/

# Signing material — NEVER commit keys or keystore credentials
*.jks
*.keystore
*.p12
*.pem
*.key
keystore.properties
key.properties
signing.properties

# IDE / OS
.idea/
*.iml
.DS_Store
