# Copy to keystore.properties (gitignored) to sign release builds.
# Keep the keystore file itself OUTSIDE this repository.
# Without these, a release build is left UNSIGNED on purpose — see SECURITY.md.
storeFile=/absolute/path/outside/the/repo/schattenweg-release.jks
storePassword=
keyAlias=schattenweg
keyPassword=
